using UnityEngine;

public class RotateMe2 : MonoBehaviour
{
    public RotateMe2 rotateMe2;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
